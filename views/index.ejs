<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>
  <body>
    <h1><%= title %></h1>
    <form>
      <button type="submit" class="btn btn-default"> <a href="/add/">Add</a> </button>
    </form>

    <table class="table">
      <thead>
        <tr>
          <th>No</th>
          <th>TITLE</th>
          <th>COMPLETED</th>
          <th>EMAIL</th>
          <th>CREATED AT</th>
          <th>ACTION</th>
        </tr>
      </thead>
      <tbody>
        <% for (var i=0;i < todo.length;i++) { %>
        <tr>
          <th scope="row"> <%= i + 1 %> </th>
          <td><%= todo[i].title %></td>
          <td><%= todo[i].is_complete %></td>
          <!-- <td><%= JSON.stringify(todo[i].toJSON()) %></td> -->
          <td><%= todo[i].User.email %></td>
          <td><%= todo[i].createdAt %></td>
          <td><button type="submit" name="submit" class="btn btn-info">update</button>  <button type="submit" name="submit" class="btn btn-danger">delete</button> </td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </body>
</html>
